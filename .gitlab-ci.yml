stages:
  - build
  - package
  - deploy

build:
  stage: build
  image: node:latest
  only:
    - test_ci
  script:
    - npm install -g @angular/cli@
    - npm install
    - ng build
  artifacts:
    paths:
      - dist/
  cache:
    paths:
      - node_modules/
