stages:
  - build
  - package
  - deploy

build:
  stage: build
  image: node:latest
  only:
    - test
  script:
    - npm install -g @angular/cli@9.1.0
    - npm install
    - ng build
  artifacts:
    paths:
      - dist/
    expire_in: 2 hours
  cache:
    paths:
      - node_modules/
